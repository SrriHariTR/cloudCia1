apiVersion: v1
kind: Service
metadata:
  name: smart-cold-chain-frontend-service
spec:
  type: NodePort
  selector:
    app: smart-cold-chain-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080